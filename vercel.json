{
  "buildCommand": "npm install --prefix \"vins website update\" && npm --prefix \"vins website update\" run build",
  "outputDirectory": "vins website update/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
